// CREATE SVG FROM RAW HTML ononline


// Here is a raw SVG I copied from the web.  
// Please clean it so I can save it as a standalone `.svg` file in my Nuxt project.  

// - Keep all paths/lines/shapes exactly the same.  
// - Remove useless attributes like `class="..."`, `xmlns:xlink`, inline styles I don’t need.  
// - Make sure it starts with <svg xmlns="http://www.w3.org/2000/svg" ...> so the file is valid.  
// - Keep the original stroke/fill colors.  
// - Output only cleaned code, nothing else.  


// - (Optional) Give me a second version that replaces stroke/fill colors with `currentColor` so I can style it with Tailwind (`text-red-500`, etc.).  